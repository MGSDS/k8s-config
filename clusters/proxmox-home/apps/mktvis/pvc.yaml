apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mktvis-data
  namespace: mktvis
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 500Mi

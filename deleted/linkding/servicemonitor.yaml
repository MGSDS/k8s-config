apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: linkding
  namespace: monitoring
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: linkding
  namespaceSelector:
    matchNames:
      - linkding
  endpoints:
    - port: http
      interval: 30s
      path: /metrics
      scrapeTimeout: 10s
